name: self-hosted-runner-workflow

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  job1:
    runs-on: k3s-arc
    steps:
      - name: Display system information
        run: |
          echo "Running on a self-hosted runner!"
          echo "Operating System: $(uname -a)"
          echo "CPU Architecture: $(uname -m)"
          echo "Available Memory: $(free -h)"
          echo "Runner Name: ${{ runner.name }}"
          sleep 90

  job2:
    runs-on: k3s-arc
    steps:
     - name: Display system information
       run: |
         echo "Running on a self-hosted runner!"
         echo "Operating System: $(uname -a)"
         echo "CPU Architecture: $(uname -m)"
         echo "Available Memory: $(free -h)"
         echo "Runner Name: ${{ runner.name }}"
         sleep 90
  
  # python-job:
  #   runs-on: github-actions-scaleset-demo
  #   steps:
  #     - name: Set up Python
  #       uses: actions/setup-python@v6
  #       with:
  #         python-version: '3.x'
  #     - name: Install dependencies
  #       run: |
  #         python -m pip install --upgrade pip
  #         pip install requests
  #     - name: Run a Python script
  #       run: |
  #         echo "Running a Python script on a self-hosted runner!"
  #         python -c "import requests; print(requests.__version__)"
  
  # nodejs-job:
  #   runs-on: github-actions-scaleset-demo
  #   steps:
  #     - name: Set up Node.js
  #       uses: actions/setup-node@v6
  #       with:
  #         node-version: '24'
  #     - name: Install dependencies
  #       run: |
  #         npm install axios
  #     - name: Run a Node.js script
  #       run: |
  #         echo "Running a Node.js script on a self-hosted runner!"
  #         node -e "const axios = require('axios'); console.log(axios.VERSION);"
  
  